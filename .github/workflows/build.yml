name: Gradle Build & UT

on: [push, pull_request, workflow_dispatch]

jobs:
#  build:
#    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-build.yml@v2.1.7
#    with:
#      flow-type: public
#      java-version: '8'

  publish_and_test:
    runs-on: ubuntu-latest
#    needs: build

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'adopt'
          cache: 'gradle'

      - name: Cache local Maven repository
        uses: actions/cache@v2
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Publish to Maven Local
        run: ./gradlew build publishAllPublicationsToMavenLocalRepository

      - name: Check .m2 repository contents
        run: ls -R ~/.m2/repository/org/octopusden/octopus || echo "Directory does not exist"

      - name: Run tests
        env:
          LICENSE_REGISTRY_GIT_REPOSITORY: "https://github.com/aksetiyawan/license-registry-test.git"
          SUPPORTED_GROUPS: "org.octopusden.octopus"
        working-directory: ft
        run: ./gradlew test --info -s